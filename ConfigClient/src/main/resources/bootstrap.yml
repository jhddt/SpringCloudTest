spring:
  application:
    name: demo
  profiles:
    active: dev
  config:
    import: "optional:configserver:http://192.168.159.128:9005"
  cloud:
    config:
      label: master
      profile: ${spring.profiles.active}
  rabbitmq:
    host: 192.168.159.128
    port: 5672
    username: admin
    password: rabbitpass

---
spring:
  config:
    activate:
      on-profile: dev

---
spring:
  config:
    activate:
      on-profile: pro
